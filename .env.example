# ============================================================================
# aida-voice — Environment Variables
# ============================================================================
# Voice gateway on port 3979. Handles ACS calling, Realtime API bridge,
# wake-word detection, and voice tool execution.
# Copy to .env and fill in <SECRET> values from Key Vault (aida-kv-poc).

# ── Azure Communication Services (ACS Calling) ──────────────────────────────
ACS_CONNECTION_STRING=<SECRET:acs-connection-string>
ACS_RESOURCE_ID=<SECRET:acs-resource-id>
ACS_CALLBACK_HOST=https://aida-bot.victoriousstone-0926fdf3.eastus.azurecontainerapps.io

# ── Azure OpenAI Realtime (Voice ↔ GPT Bridge) ──────────────────────────────
AZURE_OPENAI_REALTIME_ENDPOINT=<SECRET:openai-realtime-endpoint>
AZURE_OPENAI_REALTIME_API_KEY=<SECRET:openai-realtime-api-key>
AZURE_OPENAI_REALTIME_DEPLOYMENT=gpt-realtime
AZURE_OPENAI_REALTIME_API_VERSION=2025-04-01-preview

# ── Azure OpenAI (Chat — for voice tool fallback) ────────────────────────────
AZURE_OPENAI_ENDPOINT=https://aida-openai-poc.openai.azure.com/
AZURE_OPENAI_API_KEY=<SECRET:openai-api-key>
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-10-21

# ── Voice Config ──────────────────────────────────────────────────────────────
VOICE_MODE=realtime
AIDA_VOICE=sage
AIDA_VAD_THRESHOLD=0.4
AIDA_SILENCE_DURATION_MS=300
AIDA_PREFIX_PADDING_MS=300
AIDA_MAX_RESPONSE_TOKENS=4096
AIDA_VOICE_TEMPERATURE=0.6
VOICE_FALLBACK_ENABLED=true

# ── Media Bridge (.NET VM — for Graph-joined meetings) ────────────────────────
MEDIA_BRIDGE_URL=http://aida-media-bridge.eastus.cloudapp.azure.com:8080

# ── Microsoft Graph (for call context, speaker lookup) ────────────────────────
GRAPH_TENANT_ID=98fb8319-b410-4961-ab13-adb406303873
GRAPH_CLIENT_ID=4ec1b314-9024-4314-ad11-7a53e2040f38
GRAPH_CLIENT_SECRET=<SECRET:graph-client-secret>
GRAPH_USER_EMAIL=<SECRET:graph-user-email>

# ── Inter-Service URLs ────────────────────────────────────────────────────────
DATA_SERVICE_URL=http://localhost:3981
INTELLIGENCE_SERVICE_URL=http://localhost:3980

# ── Key Vault (production secret loading) ─────────────────────────────────────
KEY_VAULT_NAME=aida-kv-poc
AZURE_CLIENT_ID=

# ── Application Insights ─────────────────────────────────────────────────────
APPINSIGHTS_CONNECTION_STRING=

# ── Employee Profile (for voice persona context) ──────────────────────────────
EMPLOYEE_NAME=Praveen Govindaraj
EMPLOYEE_ID=emp-001
COMPANY_NAME=NCS
JOB_TITLE=Engineer

# ── App ───────────────────────────────────────────────────────────────────────
PORT=3979
LOG_LEVEL=info
